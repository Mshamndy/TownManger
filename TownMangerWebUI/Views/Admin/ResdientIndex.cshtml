@model  IEnumerable< TownManger.Domain.Entities.Resdient>

@{
    ViewBag.Title = "Admin : All Resdients";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="panel panel-default">
    @*<div class="panel-heading">
            <h3>All Resdients</h3>
        </div>
        <div class="panel-body">
            <table class="table table-striped table-condensed table-bordered">
                <tr>
                    <th class="text-right">ID</th>
                    <th>Name</th>
                    <th class="text-right">Unit Number</th>
                    <th class="text-center">Actions</th>
                </tr>
                @foreach (var item in Model)
                {
                    <tr>
                        <td class="text-right">@item.ResdientID</td>
                        <td>@Html.ActionLink(item.ResdientName, "ResdientEdit", new { item.ResdientID })</td>
                        <td class="text-right">
                        @Html.DisplayFor(m => item.Unit.UnitID)</td>
                        <td class="text-center">
                            @using (Html.BeginForm("Delete", "Admin"))
                            {
                                @Html.Hidden("resdientID", item.ResdientID)
                                <input type="submit"
                                       class="btn btn-default btn-xs"
                                       value="Delete" />
                            }
                        </td>
                    </tr>
                }
            </table>
        </div>*@

 
    @foreach (var p in Model)
    {
        @Html.Partial("ResdientSummry", p)
    }
    <div class="panel-footer">
        @Html.ActionLink("Add a new Resdient", "CreateResdient", null,
        new { @class = "btn btn-default" })
    </div>
</div>

